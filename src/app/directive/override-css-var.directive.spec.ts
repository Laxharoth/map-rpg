import { OverrideCssVarDirective } from './override-css-var.directive';

describe('OverrideCssVarDirective', () => {
  it('should create an instance', () => {
    const directive = new OverrideCssVarDirective();
    expect(directive).toBeTruthy();
  });
});
